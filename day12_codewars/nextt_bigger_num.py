def next_bigger(n):
    # your code here

    l = [int(i) for i in str(n)]  # [1, 3, 5] #l[-(len(l), -(len(l+1), -(len(l+2)]
    for i in l:
        current_index = list.index(i)
        next_index = current_index + 1
        if current_index > 0:





    # l = l.reverse()  # [5, 3, 1]
    for i in l:
        if l[current_index] ==
        elif l[current_index] == -2
        elif l[current_index] != l[-2] and l[current_index] > l[next_index]  # 5 > 3
